---
phase: 03-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - HERO-06

must_haves:
  truths:
    - "Hero renders the Nam Nam logo (NamNamPizza&Grill.png) large and centered at the top"
    - "Tagline 'Gatekjøkkenet i Åmot – Fastfood som du ønsker, når du ønsker!' is visible below the logo"
    - "Subtext 'Du ringer – Vi bringer! 📞 41 23 22 19' is visible"
    - "'RING OG BESTILL' button links to tel:+4741232219"
    - "'SE MENYEN' button links to #meny and smooth-scrolls to the menu section"
    - "All hero content fades in and slides up on first load (motion animation)"
  artifacts:
    - path: "src/components/Hero.tsx"
      provides: "Hero section with logo, tagline, CTAs, and motion animation"
      contains: "use client"
  key_links:
    - from: "src/components/Hero.tsx"
      to: "motion/react"
      via: "import { motion } from 'motion/react'"
      pattern: "motion\\.div"
    - from: "src/components/Hero.tsx"
      to: "#meny"
      via: "href='#meny' on SE MENYEN button"
      pattern: "href.*meny"
---

<objective>
Rewrite `src/components/Hero.tsx` with the real Nam Nam Pizza & Grill hero content: logo, tagline, subtext, two CTA buttons, and a fade-in/slide-up entrance animation using the `motion` library.

Purpose: The current Hero.tsx has template placeholders ([HERO_HEADLINE], placeholder image, GitHub button). This plan replaces it entirely with the restaurant's brand content.
Output: A complete `Hero.tsx` that is `"use client"`, uses `motion/react` for animation, renders the logo via `next/image`, and provides both CTA buttons.
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-RESEARCH.md
@src/components/Hero.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion library</name>
  <files>package.json</files>
  <action>
Run `npm install motion` in the project root. The `motion` package (not `framer-motion` — that is the old name) is the locked decision from STATE.md for hero animation only. This adds `motion` to package.json dependencies.

Verify the install succeeded by checking that `motion` appears in package.json dependencies.
  </action>
  <verify>
    <automated>cd /Users/steffen/Desktop/Projects/website_template && node -e "require('motion/react')" 2>&1 || npm ls motion 2>&1 | head -5</automated>
  </verify>
  <done>`motion` appears in node_modules and package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Hero.tsx with real content and motion animation</name>
  <files>src/components/Hero.tsx</files>
  <action>
Completely replace the contents of `src/components/Hero.tsx`. The new implementation:

**File structure:**
```
"use client"
import Image from "next/image"
import { motion } from "motion/react"
```

**Layout:** Single-column centered layout (not the old two-column left/right layout). Full-viewport-height section with dark background, content centered vertically and horizontally.

**Structure:**
```tsx
"use client";
import Image from "next/image";
import { motion } from "motion/react";

export const Hero = () => {
  return (
    <section className="relative flex items-center justify-center min-h-[calc(100vh-80px)] bg-neutral-950 overflow-hidden px-4 py-16">
      <div className="flex flex-col items-center text-center max-w-2xl mx-auto gap-6">
        {/* Logo — HERO-01 */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, ease: "easeOut" }}
        >
          <Image
            src="/img/NamNamPizza&Grill.png"
            alt="Nam Nam Pizza & Grill"
            width={280}
            height={280}
            priority
            className="w-[180px] h-auto lg:w-[280px]"
          />
        </motion.div>

        {/* Tagline + subtext — HERO-02, HERO-03 */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, delay: 0.2, ease: "easeOut" }}
          className="flex flex-col gap-3"
        >
          <h1 className="text-2xl font-bold text-white lg:text-4xl leading-tight">
            Gatekjøkkenet i Åmot – Fastfood som du ønsker, når du ønsker!
          </h1>
          <p className="text-lg text-gray-300 lg:text-xl">
            Du ringer – Vi bringer! 📞 41 23 22 19
          </p>
        </motion.div>

        {/* CTA buttons — HERO-04, HERO-05 */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, delay: 0.4, ease: "easeOut" }}
          className="flex flex-col sm:flex-row gap-4 items-center"
        >
          <a
            href="tel:+4741232219"
            className="px-8 py-4 bg-orange-600 hover:bg-orange-500 text-white font-bold text-lg rounded-md transition-colors duration-200 w-full sm:w-auto text-center"
          >
            RING OG BESTILL
          </a>
          <a
            href="#meny"
            className="px-8 py-4 bg-neutral-800 hover:bg-neutral-700 text-white font-semibold text-lg rounded-md transition-colors duration-200 w-full sm:w-auto text-center border border-neutral-700"
          >
            SE MENYEN
          </a>
        </motion.div>
      </div>
    </section>
  );
};
```

Important notes:
- `"use client"` must be the very first line (before imports) — required for motion/react browser APIs
- Use `next/image` with `width={280} height={280}` and `className="w-[180px] h-auto lg:w-[280px]"` — the props control intrinsic size/aspect ratio, the className controls responsive display size (HERO-01)
- The logo source path is `/img/NamNamPizza&Grill.png` (matches the public/img directory)
- Staggered motion: logo at delay 0, tagline at delay 0.2, buttons at delay 0.4 (creates a natural cascade)
- `min-h-[calc(100vh-80px)]` accounts for the ~80px sticky navbar height
- Do NOT use `framer-motion` — that is the old package name. Use `motion/react` import from `motion` package
- The `#meny` href works because page.tsx has `<section id="meny">` and `scroll-behavior: smooth` is set in globals.css
  </action>
  <verify>
    <automated>cd /Users/steffen/Desktop/Projects/website_template && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Grep for "use client" at top of Hero.tsx, "tel:+4741232219" present, "#meny" href present, "motion/react" import present, "NamNamPizza" image src present</manual>
  </verify>
  <done>TypeScript compiles without errors; Hero.tsx has "use client", imports motion from "motion/react", renders NamNamPizza&Grill.png logo, tagline, subtext, RING OG BESTILL tel: link, SE MENYEN #meny link</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes with 0 errors.
`npm run lint` passes.
Hero.tsx first line is `"use client"`.
All 6 HERO requirements are addressed: logo (HERO-01), tagline (HERO-02), subtext (HERO-03), tel: button (HERO-04), #meny button (HERO-05), motion animation (HERO-06).
</verification>

<success_criteria>
Hero.tsx compiled, "use client" at top, motion/react animation on 3 staggered elements, correct logo path, Norwegian tagline and subtext, two correctly-linked CTA buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-02-SUMMARY.md`
</output>
