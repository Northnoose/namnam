---
phase: 03-content-sections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.tsx
autonomous: true
requirements:
  - FOOT-01
  - FOOT-02
  - FOOT-03
  - FOOT-04
  - FOOT-05
  - FOOT-06

must_haves:
  truths:
    - "Footer shows address 'Strandgata 11, 3340 Åmot'"
    - "Phone number in footer is a clickable tel:+4741232219 link"
    - "Opening hours 'Alle dager 13:00–23:00' are visible in footer"
    - "Facebook link to facebook.com/NumNumPizzaGrill is present"
    - "Text 'Langs Riksvei 287 – Parkering utenfor!' is visible in footer"
    - "Copyright line shows 'Nam Nam Pizza & Grill' and the current year"
    - "Footer root element has id='kontakt' for anchor navigation"
  artifacts:
    - path: "src/components/Footer.tsx"
      provides: "Footer/Kontakt section with all business contact info"
      contains: "id=\"kontakt\""
  key_links:
    - from: "src/components/Footer.tsx"
      to: "tel:+4741232219"
      via: "href on phone link"
      pattern: "tel:\\+4741232219"
    - from: "src/components/Footer.tsx"
      to: "facebook.com/NumNumPizzaGrill"
      via: "href on Facebook link"
      pattern: "facebook.com/NumNumPizzaGrill"
    - from: "Navbar #kontakt link"
      to: "Footer id='kontakt'"
      via: "anchor scroll"
      pattern: "id=\"kontakt\""
---

<objective>
Rewrite `src/components/Footer.tsx` with the real Nam Nam Pizza & Grill contact information, replacing all template placeholders.

Purpose: The current Footer.tsx has [COMPANY_NAME], [FOOTER_DESCRIPTION], fake navigation links (Product, Features, Pricing), and placeholder social media icons. This plan replaces it entirely with real restaurant content.
Output: A clean footer with address, phone, hours, Facebook link, parking note, and copyright. The root element gets `id="kontakt"` so the Navbar's "#kontakt" anchor scroll works (note: the RESEARCH.md recommendation was to add id="kontakt" to Footer's root element rather than modifying layout.tsx).
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-RESEARCH.md
@src/components/Footer.tsx
@src/app/layout.tsx
@BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Footer.tsx with real Nam Nam contact info</name>
  <files>src/components/Footer.tsx</files>
  <action>
Completely replace the contents of `src/components/Footer.tsx`. The new implementation:

```tsx
import { Container } from "@/components/Container";

export function Footer() {
  return (
    // id="kontakt" enables the Navbar #kontakt anchor link to scroll here
    <footer id="kontakt" className="bg-neutral-900 border-t border-neutral-800 mt-8">
      <Container className="py-12">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">

          {/* Brand + description */}
          <div className="lg:col-span-2">
            <h2 className="text-xl font-bold text-white mb-2">
              Nam Nam Pizza &amp; Grill
            </h2>
            <p className="text-gray-400 text-sm leading-relaxed mb-4">
              Gatekjøkkenet i Åmot – Fastfood som du ønsker, når du ønsker!
              Vi leverer innenfor Modum kommune.
            </p>
            {/* FOOT-05: Parking note */}
            <p className="text-gray-500 text-sm">
              Langs Riksvei 287 – Parkering utenfor!
            </p>
          </div>

          {/* Contact info */}
          <div>
            <h3 className="text-white font-semibold mb-4">Kontakt</h3>
            <div className="flex flex-col gap-3 text-sm">
              {/* FOOT-01: Address */}
              <div>
                <p className="text-gray-400">📍 Strandgata 11, 3340 Åmot</p>
              </div>
              {/* FOOT-02: Phone — clickable tel: link */}
              <div>
                <a
                  href="tel:+4741232219"
                  className="text-orange-400 hover:text-orange-300 font-medium transition-colors duration-200"
                >
                  📞 41 23 22 19
                </a>
              </div>
              {/* FOOT-04: Facebook link */}
              <div>
                {/* TODO: verify Facebook URL — BRIEF.md says NumNumPizzaGrill, restaurant name is NamNam */}
                <a
                  href="https://facebook.com/NumNumPizzaGrill"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="text-blue-400 hover:text-blue-300 transition-colors duration-200"
                >
                  Facebook
                </a>
              </div>
            </div>
          </div>

          {/* Opening hours */}
          <div>
            <h3 className="text-white font-semibold mb-4">Åpningstider</h3>
            {/* FOOT-03: Opening hours */}
            <div className="text-sm">
              <div className="flex justify-between gap-4">
                <span className="text-gray-400">Alle dager</span>
                <span className="text-white font-medium">13:00–23:00</span>
              </div>
            </div>
          </div>

        </div>

        {/* FOOT-06: Copyright */}
        <div className="mt-10 pt-6 border-t border-neutral-800 text-center text-sm text-gray-500">
          Copyright &copy; {new Date().getFullYear()} Nam Nam Pizza &amp; Grill. Alle rettigheter forbeholdt.
        </div>
      </Container>
    </footer>
  );
}
```

Important notes:
- The root element must be `<footer id="kontakt">` — this makes the Navbar "#kontakt" anchor link work. layout.tsx renders `<Footer />` outside of `{children}`, so the footer is already in the DOM; the id is needed for scroll targeting.
- Keep the `export function Footer()` named export — layout.tsx imports it as `{ Footer }`.
- Remove all template imports (Link, Image from this file — not needed in new design).
- The Facebook URL uses `facebook.com/NumNumPizzaGrill` as given in BRIEF.md, with a TODO comment noting the URL discrepancy flagged in STATE.md.
- Use `{new Date().getFullYear()}` for the copyright year — evaluated at server render time.
- Do NOT include the old navigation arrays (Product, Features, Pricing) or social media SVG icons (Twitter, Instagram, LinkedIn) — fully replaced.
  </action>
  <verify>
    <automated>cd /Users/steffen/Desktop/Projects/website_template && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check Footer.tsx: id="kontakt" on root, "Strandgata 11", "tel:+4741232219", "13:00–23:00", "NumNumPizzaGrill", "Parkering utenfor", "Nam Nam Pizza" in copyright</manual>
  </verify>
  <done>Footer.tsx rewritten with id="kontakt", real address, tel: phone link, opening hours, Facebook link, parking note, and copyright — TypeScript compiles cleanly (FOOT-01 through FOOT-06)</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes with 0 errors.
Footer.tsx root element has `id="kontakt"`.
All 6 FOOT requirements addressed: address (FOOT-01), tel: phone (FOOT-02), hours (FOOT-03), Facebook link (FOOT-04), parking note (FOOT-05), copyright (FOOT-06).
</verification>

<success_criteria>
Footer.tsx fully replaced, compiles cleanly, id="kontakt" anchor present, all 6 FOOT requirements satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-04-SUMMARY.md`
</output>
