---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
autonomous: false
files_modified:
  - src/components/Navbar.tsx
requirements:
  - FOUND-05

must_haves:
  truths:
    - "Site layout renders without horizontal overflow at 320px viewport width"
    - "Navbar logo and hamburger button fit within 320px without clipping"
    - "Layout renders correctly at 768px (tablet) and 1280px (desktop)"
    - "No horizontal scrollbar appears at any of the three tested breakpoints"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Responsive navbar padding that works at 320px"
  key_links:
    - from: "src/components/Navbar.tsx"
      to: "320px viewport"
      via: "Tailwind responsive padding classes"
      pattern: "px-4.*lg:px-8|p-4.*lg:p-8"
---

<objective>
Verify (and fix if needed) that the site layout is responsive at 320px, 768px, and 1280px. The primary risk is the navbar at 320px — current `p-8` padding (32px each side) leaves only 256px for content, which may cause the 120px logo + hamburger button to overflow.

Purpose: FOUND-05 is a prerequisite for all phase 2 and 3 work. Navigation and content sections will break if the base layout has horizontal overflow at mobile widths.
Output: Human-confirmed responsive layout. Navbar.tsx possibly updated with responsive padding.
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix navbar padding for 320px viewport (if overflow present)</name>
  <files>src/components/Navbar.tsx</files>
  <action>
The navbar nav element currently has `p-8` (32px padding on all sides). At 320px viewport: 320 - 64 (left+right padding) = 256px. With a 120px logo + spacing + hamburger button (~50px), this may overflow.

Run the dev server and check in DevTools at 320px width. Look for horizontal scrollbar or content clipping.

**If overflow exists:** Change the nav padding from `p-8` to `px-4 py-4 lg:px-8`:
```tsx
// Before
<nav className="container relative flex flex-wrap items-center justify-between p-8 mx-auto lg:justify-between xl:px-1">

// After (if 320px overflows)
<nav className="container relative flex flex-wrap items-center justify-between px-4 py-4 lg:px-8 mx-auto lg:justify-between xl:px-1">
```

**If no overflow:** Do not change the padding. Leave Navbar.tsx as-is from Plan 01.

In either case, confirm no overflow at:
- 320px — minimum mobile (iPhone SE viewport)
- 768px — Tailwind md: breakpoint (tablet)
- 1280px — Tailwind xl: breakpoint (desktop)

Use `npm run dev` and DevTools device toolbar to test each width.
  </action>
  <verify>
    <automated>cd /Users/steffen/Desktop/Projects/website_template && npx tsc --noEmit</automated>
    <manual>Start dev server (npm run dev). Open http://localhost:3000 in Chrome. Open DevTools → Toggle device toolbar. Test at exactly 320px, 768px, 1280px. At each width: confirm no horizontal scrollbar, navbar items are not clipped, content is readable.</manual>
  </verify>
  <done>No horizontal overflow at 320px, 768px, or 1280px. If padding was changed, Navbar.tsx has px-4 py-4 lg:px-8. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify all Phase 1 foundation changes</name>
  <action>Checkpoint: human visually verifies FOUC prevention, lang=nb, Nam Nam logo, and responsive layout at 320/768/1280px.</action>
  <what-built>
    Complete Phase 1 foundation changes:
    - layout.tsx: dark theme FOUC fix (defaultTheme="dark", enableSystem=false) + lang="nb"
    - Navbar.tsx: Nam Nam PNG logo via next/image (120x120, priority)
    - src/data/menu.ts: TypeScript interfaces + 5-category stub array
    - Navbar responsive padding (adjusted if needed for 320px)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000

    2. FOUC test — open in Incognito, hard reload (Ctrl+Shift+R or Cmd+Shift+R):
       - Expected: dark background visible immediately, NO white flash

    3. Language test — open DevTools → Elements, find `<html>`:
       - Expected: `<html lang="nb" class="dark" ...>`

    4. Logo test — look at the navbar:
       - Expected: Nam Nam PNG logo visible (not placeholder SVG or "[COMPANY_NAME]" text)

    5. Responsive test — DevTools → Toggle device toolbar:
       - At 320px: no horizontal scrollbar, logo + hamburger fit
       - At 768px: layout renders correctly
       - At 1280px: desktop nav links visible

    6. TypeScript: run `npx tsc --noEmit` in terminal:
       - Expected: exits 0 with no errors

    Type "approved" if all checks pass, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md:
1. Page loads dark on first visit — no white flash in Incognito
2. DevTools shows `<html lang="nb">` in the DOM
3. Logo renders without layout shift (next/image with explicit dimensions)
4. `src/data/menu.ts` exists with typed MenuCategory[] covering 5 categories
5. Layout renders correctly at 320px, 768px, and 1280px
</verification>

<success_criteria>
- All 5 ROADMAP Phase 1 success criteria confirmed by human
- npx tsc --noEmit exits 0
- No horizontal overflow at 320px
- Human approves visual inspection of dark theme, lang, logo, and responsiveness
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
