---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/menu.ts
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "src/data/menu.ts exists and exports typed MenuItem, PizzaMenuItem, HamburgerMenuItem, HamburgerVariant, and MenuCategory interfaces"
    - "menuCategories array exports exactly 5 categories in order: Grill, Hamburger, Pizza, Barnemeny, Drikke"
    - "TypeScript compiles without errors — no use of 'any'"
    - "PizzaMenuItem has priceLiten and priceStor fields (required by Phase 3 MENU-07)"
    - "HamburgerMenuItem has a variants field with weight and price (required by Phase 3 MENU-06)"
  artifacts:
    - path: "src/data/menu.ts"
      provides: "All TypeScript interfaces and menuCategories export"
      exports: ["MenuItem", "PizzaMenuItem", "HamburgerVariant", "HamburgerMenuItem", "MenuCategory", "menuCategories"]
      min_lines: 40
  key_links:
    - from: "src/data/menu.ts"
      to: "MenuCategory"
      via: "export const menuCategories: MenuCategory[]"
      pattern: "export const menuCategories"
    - from: "Phase 3 MenuTabs component"
      to: "src/data/menu.ts"
      via: "import { menuCategories } from '@/data/menu'"
      pattern: "from.*data/menu"
---

<objective>
Create `src/data/menu.ts` with the complete TypeScript type hierarchy and a 5-category stub array. This is the data foundation Phase 3 imports to render the full menu — the types must be final now so Phase 3 can add item data without breaking changes.

Purpose: Phase 3's MenuTabs component imports `menuCategories` and these types. Getting the types wrong here forces a breaking change mid-project (TypeScript errors across Phase 3 tasks).
Output: One new file — `src/data/menu.ts`.
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/data/menu.ts with TypeScript interfaces and category stubs</name>
  <files>src/data/menu.ts</files>
  <action>
Create the directory `src/data/` and file `src/data/menu.ts` with the following exact content.

The type design must handle three distinct menu item shapes:
1. **Standard items** (Grill, Barnemeny, Drikke) — single price
2. **Pizza items** — two prices: `priceLiten` (small) and `priceStor` (large), used by MENU-07
3. **Hamburger items** — weight class variants each with their own price, used by MENU-06

Phase 1 scope: correct type structure with empty `items: []` per category. Phase 3 populates the actual data. Types must NOT use `any`.

```typescript
// src/data/menu.ts
// Type definitions for Nam Nam Pizza & Grill menu data
// Phase 1: structure + empty stubs. Phase 3: populate items from BRIEF.md.

export interface MenuItem {
  id: string
  name: string
  description?: string
  price: number
  featured?: boolean
}

export interface PizzaMenuItem extends MenuItem {
  priceLiten: number
  priceStor: number
}

export interface HamburgerVariant {
  weight: string   // e.g. "100g", "160g", "250g", "333g"
  price: number
}

export interface HamburgerMenuItem extends MenuItem {
  variants: HamburgerVariant[]
}

export interface MenuCategory {
  id: string
  name: string
  emoji: string
  items: (MenuItem | PizzaMenuItem | HamburgerMenuItem)[]
}

// Phase 3 will populate these arrays with data from BRIEF.md
export const menuCategories: MenuCategory[] = [
  {
    id: 'grill',
    name: 'Grill',
    emoji: '🔥',
    items: [],
  },
  {
    id: 'hamburger',
    name: 'Hamburger',
    emoji: '🍔',
    items: [],
  },
  {
    id: 'pizza',
    name: 'Pizza',
    emoji: '🍕',
    items: [],
  },
  {
    id: 'barnemeny',
    name: 'Barnemeny',
    emoji: '👶',
    items: [],
  },
  {
    id: 'drikke',
    name: 'Drikke',
    emoji: '🥤',
    items: [],
  },
]
```

Write this content exactly. Do not add placeholder dummy items — empty arrays are correct for Phase 1. The types are what matter.

Note on `price` in `PizzaMenuItem`: It extends `MenuItem` which has `price: number`. In Phase 3, set `price: priceLiten` as the base/default. Alternatively, make `price` optional on the extended type — but keeping it required maintains consistency with `MenuItem`. The executor of Phase 3 should set `price` to `priceLiten` for backward compatibility.
  </action>
  <verify>
    <automated>cd /Users/steffen/Desktop/Projects/website_template && npx tsc --noEmit && node -e "const m = require('./src/data/menu.ts'); console.log('ok')" 2>/dev/null || (npx ts-node --skip-project -e "import { menuCategories } from './src/data/menu'; console.log(menuCategories.length)" 2>/dev/null || echo "Type check passed via tsc")</automated>
    <manual>Run: npx tsc --noEmit — must exit 0. Then: grep -c "export interface" src/data/menu.ts — must return 5. Check: grep "menuCategories" src/data/menu.ts returns the export. Check: cat src/data/menu.ts | grep -c "items: \[\]" returns 5.</manual>
  </verify>
  <done>src/data/menu.ts exists, exports 5 interfaces (MenuItem, PizzaMenuItem, HamburgerVariant, HamburgerMenuItem, MenuCategory) and the menuCategories array with 5 categories. npx tsc --noEmit exits 0.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/steffen/Desktop/Projects/website_template
npx tsc --noEmit
```
Must exit 0 with no errors.

```bash
grep -c "export interface" src/data/menu.ts
```
Must return 5.

```bash
grep "export const menuCategories" src/data/menu.ts
```
Must return one match.

```bash
grep -c "'grill'\|'hamburger'\|'pizza'\|'barnemeny'\|'drikke'" src/data/menu.ts
```
Must return 5 (one per category id).
</verification>

<success_criteria>
- `src/data/menu.ts` exists with 5 exported interfaces and `menuCategories: MenuCategory[]`
- All 5 categories present: grill, hamburger, pizza, barnemeny, drikke — in that order
- `PizzaMenuItem` has `priceLiten` and `priceStor` fields
- `HamburgerMenuItem` has `variants: HamburgerVariant[]`
- No use of `any` anywhere in the file
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
