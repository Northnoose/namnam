---
phase: 02-navigation-shell
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]

must_haves:
  truths:
    - "All four navbar section links scroll to correct sections with heading fully visible (not behind navbar)"
    - "Navbar background is transparent at page top and becomes solid dark after scrolling 50px+"
    - "Mobile hamburger opens a panel with four section links; tapping a link closes the panel and scrolls to the section"
    - "Fixed orange call bar is visible at the bottom of the screen on mobile at all scroll positions"
    - "Tapping the call bar initiates a phone call to 41 23 22 19"
    - "Desktop navbar shows phone number button that initiates a call"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Verified sticky scroll-aware navbar"
    - path: "src/components/CallBar.tsx"
      provides: "Verified fixed mobile call bar"
  key_links:
    - from: "Navbar anchor links"
      to: "section ids in page.tsx"
      via: "href=#section-id + scroll-padding-top"
      pattern: "href.*#om-oss|#meny|#levering|#kontakt"
---

<objective>
Human visual verification of all Phase 2 navigation features. Claude has automated all implementation — this checkpoint confirms correct visual and interactive behavior across mobile and desktop viewport widths.

Purpose: Navigation behavior (scroll transition, mobile menu open/close, call bar visibility, anchor offset) requires human eyes and interactive testing in a real browser. Claude cannot verify these visually.
Output: Human confirmation that all NAV-01 through NAV-05 requirements pass.
</objective>

<execution_context>
@/Users/steffen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steffen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-shell/02-01-SUMMARY.md
@.planning/phases/02-navigation-shell/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 2 navigation shell — visual and interactive browser testing</name>
  <what-built>
    Phase 2 Navigation Shell — all five requirements implemented:
    - NAV-01: Navbar with logo + four section links (Om oss, Meny, Levering, Kontakt) + desktop phone CTA
    - NAV-02: Sticky top-0 navbar with transparent-to-solid background transition on scroll past 50px
    - NAV-03: Mobile hamburger Disclosure menu — opens/closes, each link auto-closes the panel via Headless UI v2 CloseButton
    - NAV-04: Fixed orange call bar at bottom of screen on mobile with tel:+4741232219 link; desktop phone button in navbar right side
    - NAV-05: Smooth anchor scroll with scroll-padding-top 80px so section headings clear the sticky navbar
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:3000 in your browser. Test each item:

    **Desktop (1280px+ browser window):**
    1. Page loads — confirm navbar background is TRANSPARENT (not solid) at the top
    2. Scroll down — confirm navbar background becomes SOLID DARK (neutral-900 with blur) after ~50px of scroll
    3. Scroll back to top — confirm navbar returns to transparent
    4. Confirm four nav links visible in the desktop navbar: Om oss, Meny, Levering, Kontakt
    5. Confirm orange phone button "41 23 22 19" is visible in the desktop navbar top-right
    6. Click the phone button — confirm `tel:+4741232219` is triggered (browser asks to call or shows number)
    7. Click "Om oss" link — confirm smooth scroll to #om-oss section with heading NOT hidden behind navbar
    8. Click "Meny" link — confirm smooth scroll to #meny section
    9. Confirm NO fixed orange call bar at the bottom on desktop
    10. Confirm ThemeChanger (dark/light switch) is visible on desktop

    **Mobile (375px or DevTools device emulation):**
    11. Confirm NO desktop nav links visible (hidden below lg breakpoint)
    12. Confirm hamburger button (three lines) is visible next to ThemeChanger
    13. Tap hamburger — confirm a panel drops down with four links: Om oss, Meny, Levering, Kontakt
    14. Tap a link (e.g., "Meny") — confirm panel CLOSES and page scrolls to #meny with heading visible
    15. Confirm fixed orange bar at the bottom reading "📞 Ring oss: 41 23 22 19" is visible at all scroll positions
    16. Tap the orange call bar — confirm `tel:+4741232219` is triggered
    17. Confirm page content is NOT obscured by the call bar (bottom padding present)

    **Anchor offset check (both viewports):**
    18. Click any section link and confirm the placeholder text ("Om oss — klar i Phase 3" etc.) is FULLY VISIBLE and not hidden behind the navbar bar
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
  <action>Human runs `npm run dev`, opens http://localhost:3000, and follows the verification checklist in how-to-verify across desktop and mobile viewports. No code changes — this is a pure verification task.</action>
  <verify>Human confirms all 18 checklist items pass and types "approved" to resume execution.</verify>
  <done>All NAV-01 through NAV-05 requirements confirmed working in a live browser. Human has typed "approved".</done>
</task>

</tasks>

<verification>
All NAV-01 through NAV-05 requirements verified by human in a live browser. No automated verification possible for visual/interactive behavior.
</verification>

<success_criteria>
- Human confirms: navbar transparent at top, solid on scroll (NAV-02)
- Human confirms: four section links present and functional on desktop (NAV-01)
- Human confirms: mobile hamburger opens/closes, links auto-close menu (NAV-03)
- Human confirms: orange call bar visible at bottom on mobile, initiates call (NAV-04)
- Human confirms: section headings fully visible after anchor scroll on both viewports (NAV-05)
- Human types "approved" to unblock Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-shell/02-03-SUMMARY.md` using the summary template.
</output>
